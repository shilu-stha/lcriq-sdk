export const start = bridge => bridge.start();
  
export const stop = bridge => bridge.stop();

export const pause = bridge => bridge.pause();

export const requestData = bridge => bridge.readData();
  